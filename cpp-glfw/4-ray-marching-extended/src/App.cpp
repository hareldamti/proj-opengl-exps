#include "Render.h"
#include "Env.h"

class App {
    private:
        EnvState& env;
        
    public:
        App(EnvState& env):
            env(env) {
                
        }
        void update(){
            
        }
        void imgui() {
        }

        void draw(){
            
        }
        ~App() {

        }
};